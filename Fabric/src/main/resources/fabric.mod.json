{
  "schemaVersion": 1,
  "id": "fleettools",
  "version": "${version}",

  "name": "FleetTools",
  "description": "Adds fundamental server commands like /home, /spawn, and /back in a clean, configurable mod built for Fabric.",
  "authors": [ "HelluPeptic" ],

  "license": "MIT",
  "icon": "icon.png",

  "environment": "*",
  "entrypoints": { "main": [ "com.fleettools.FleettoolsMod" ] },

  "mixins": [
    "fleettools.mixins.json"
  ],

  "depends": {
    "fabricloader": ">=0.18.2",
    "fabric-api": "*",
    "minecraft": "~1.21.11",
    "java": ">=21"
  }
}